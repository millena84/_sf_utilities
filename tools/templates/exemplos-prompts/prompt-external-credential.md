# Prompt — ExternalCredential

Use o prompt-base para criar um utilitário de documentação de `ExternalCredential`.

- Identificar modelo de autenticação, principals, parâmetros e referências.
- Tratar parâmetros e valores de autenticação como potencialmente sensíveis.
- Relacionar NamedCredential, PermissionSet, PermissionSetGroup e AuthProvider quando houver evidência.
- Separar existência, tipo de autenticação e valor secreto.
- Verificar XML, Metadata API e Tooling API.
- Registrar campos não suportados pela fonte consultada.
